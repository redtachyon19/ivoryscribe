# Loaded automatically by `vite build` and `build:electron` (packaged desktop
# app + web build). VITE_API_URL is public — it is baked into the shipped client
# bundle — so it is committed here, not treated as a secret.
VITE_API_URL=https://api.ivoryscribe.com
